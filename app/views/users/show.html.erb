<h1> Welcome to Poseidon Rentals <%= @user.name %>! </h1>
<br><br><br>

<h3>Your Reservations:<br></h3>
<% @user.reservations.each do |r|%>
  
    <%= link_to "#{reservation_date(r)}, #{reservation_start(r)} to #{reservation_end(r)}", reservation_path(r) %><br>
<% end %>
<br><br>

View our equipment: <%= link_to 'All Equipment', equipment_index_path %><br>

Create a Reservation: <%= link_to 'Reserve Equipment', new_reservation_path %><br><br><br>

<%= link_to 'Log Out', logout_path, method: :delete %>